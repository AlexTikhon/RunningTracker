{
  "name": "@running-tracker/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm run build --workspace=@running-tracker/contracts && npm run build --workspace=@running-tracker/fixtures",
    "build": "tsc -b && vite build",
    "dev": "vite",
    "lint": "eslint src *.ts",
    "pretest": "npm run build --workspace=@running-tracker/contracts && npm run build --workspace=@running-tracker/fixtures",
    "test": "vitest run",
    "pretypecheck": "npm run build --workspace=@running-tracker/contracts && npm run build --workspace=@running-tracker/fixtures",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@running-tracker/contracts": "0.1.0",
    "@running-tracker/fixtures": "0.1.0",
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "@types/node": "24.13.6",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "@vitejs/plugin-react": "6.1.1",
    "fake-indexeddb": "6.2.5",
    "typescript": "6.0.3",
    "vite": "8.3.0",
    "vitest": "5.0.1"
  }
}
