{
  "name": "@running-tracker/fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "prebuild": "npm run build --workspace=@running-tracker/contracts",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src",
    "pretest": "npm run build --workspace=@running-tracker/contracts",
    "test": "vitest run",
    "pretypecheck": "npm run build --workspace=@running-tracker/contracts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@running-tracker/contracts": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "24.13.6",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
